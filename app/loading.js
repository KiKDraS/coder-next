import Spinner from "./components/server-side/Spinner";

const Loading = () => {
  return (
    <Spinner classes="h-screen w-screen bg-white dark:bg-gray-900 flex items-center justify-center" />
  );
};
export default Loading;
